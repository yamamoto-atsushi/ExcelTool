# Excelファイル統合機能の改善提案

## 📋 目次
1. [優先度: 高](#優先度-高)
2. [優先度: 中](#優先度-中)
3. [優先度: 低](#優先度-低)
4. [将来的な拡張機能](#将来的な拡張機能)

---

## 優先度: 高

### 1. ファイルプレビュー機能
**現在の問題**: 統合前にどのファイルが統合されるか確認できない

**改善内容**:
- 入力パス選択後、統合対象のファイルリストを表示
- ファイル名、サイズ、行数などをプレビュー
- 統合前に最終確認できる

**実装イメージ**:
```
[統合対象ファイル]
☑ test1.xlsx (150 KB, 100行)
☑ test2.xlsx (200 KB, 150行)
☐ test3.xlsx (100 KB, 80行) ← チェックボックスで選択/除外可能
```

### 2. 進捗表示の詳細化
**現在の問題**: どのファイルを処理中か分からない

**改善内容**:
- 現在処理中のファイル名を表示
- 処理済み/総ファイル数の表示（例: "3/10 ファイル処理中..."）
- 推定残り時間の表示

**実装例**:
```python
self.progress.update_message(
    f"ファイルを処理中: {file_path.name} (3/10)"
)
```

### 3. 列の不一致処理
**現在の問題**: 列が異なるファイルを統合するとエラーになる可能性

**改善内容**:
- 列の不一致を検出して警告
- オプション: 共通列のみ統合 / 全列を統合（欠損値は空）
- 統合前の列チェック機能

**実装オプション**:
- `merge_mode`: 'strict'（厳密）, 'union'（和集合）, 'intersection'（積集合）

### 4. エラーハンドリングの強化
**現在の問題**: 一部のファイルが読み込めない場合、詳細が分からない

**改善内容**:
- 失敗したファイル名とエラー理由をリスト表示
- 部分的に成功した場合の詳細情報
- エラーログファイルの出力オプション

**実装例**:
```python
{
    'success': True,  # 部分的成功も考慮
    'files_processed': 8,
    'files_failed': 2,
    'failed_files': [
        {'file': 'error.xlsx', 'reason': 'ファイルが破損しています'}
    ],
    ...
}
```

---

## 優先度: 中

### 5. 統合順序のカスタマイズ
**現在の問題**: ファイル名順のみで統合順序を制御できない

**改善内容**:
- ファイルリストのドラッグ&ドロップによる並び替え
- ソートオプション（名前、サイズ、更新日時）
- 手動での順序指定

### 6. ファイル名列の追加
**現在の問題**: 統合後、元のファイルがどれか分からない

**改善内容**:
- 統合後のデータに「元のファイル名」列を追加するオプション
- ファイルパスの一部またはフルパスを記録

**実装例**:
```python
config['add_source_column'] = True  # デフォルト: False
config['source_column_name'] = '元ファイル'  # デフォルト: '_source_file'
```

### 7. 重複データの処理
**現在の問題**: 重複行の処理方法が選べない

**改善内容**:
- 重複行を削除するオプション
- 重複行を保持するオプション
- 重複をカウントして表示

**実装オプション**:
```python
config['handle_duplicates'] = 'keep'  # 'keep', 'remove', 'count'
```

### 8. 処理前の確認ダイアログ
**現在の問題**: 統合実行前に確認できない

**改善内容**:
- 統合前にサマリーを表示
  - 統合対象ファイル数
  - 推定総行数
  - 出力ファイルパス
- 「実行」または「キャンセル」を選択

**実装例**:
```
統合を実行しますか？

統合対象: 10ファイル
推定行数: 約1,500行
出力先: merged_output.xlsx

[キャンセル]  [実行]
```

### 9. CSV形式への対応
**現在の問題**: CSVファイルが統合できない

**改善内容**:
- CSVファイルの検出と読み込み
- CSVとExcelの混合統合
- CSV形式での出力オプション

---

## 優先度: 低

### 10. 空ファイル/空シートのスキップ
**現在の問題**: 空のファイルも統合対象になる

**改善内容**:
- 空のファイルを自動スキップ
- 空のシートをスキップするオプション
- スキップしたファイルのリスト表示

### 11. 統計情報の表示
**現在の問題**: 統合後の詳細統計が分からない

**改善内容**:
- 列ごとのデータ型、欠損値数
- 数値列の統計（平均、最大、最小など）
- 統合後のサマリーレポート表示

### 12. 大きなファイルの処理最適化
**現在の問題**: メモリに全データを読み込むため、大きなファイルで問題が発生する可能性

**改善内容**:
- チャンク処理によるメモリ効率化
- 並列処理オプション
- 進捗バーの表示

### 13. バッチ処理モード
**現在の問題**: 一度に1つの統合しかできない

**改善内容**:
- 複数の統合タスクを保存
- 一括実行機能
- タスク履歴の保存

---

## 将来的な拡張機能

### 14. データ変換機能
- 統合前にデータクリーニング（空白行削除など）
- データ型の統一
- 列名の正規化

### 15. 統合テンプレート
- よく使う統合設定を保存
- ワンクリックで再実行

### 16. ドラッグ&ドロップ対応
- GUI上にファイルをドラッグ&ドロップして追加

### 17. 統合ルール定義
- 条件に基づいた統合（例: 特定の列の値でフィルタリング）
- 統合前のデータフィルタリング

### 18. 並列処理
- 複数ファイルの並列読み込み
- マルチスレッド処理による高速化

### 19. 統合履歴の保存
- 過去の統合設定を保存
- 統合結果の管理

### 20. プレビュー機能
- 統合前のデータプレビュー
- 統合後のデータプレビュー

---

## 実装の優先順位（推奨）

### Phase 1（すぐに実装すべき）
1. ✅ ファイルプレビュー機能
2. ✅ 進捗表示の詳細化
3. ✅ 列の不一致処理
4. ✅ エラーハンドリングの強化

### Phase 2（次期リリース）
5. ✅ 統合順序のカスタマイズ
6. ✅ ファイル名列の追加
7. ✅ 重複データの処理
8. ✅ 処理前の確認ダイアログ

### Phase 3（将来の拡張）
9. ✅ CSV形式への対応
10. ✅ その他の拡張機能

---

## 技術的な考慮事項

### パフォーマンス
- 大きなファイル（>100MB）の処理時間
- メモリ使用量の最適化
- 並列処理の導入検討

### ユーザビリティ
- 操作の直感性
- エラーメッセージの分かりやすさ
- ヘルプドキュメントの充実

### 保守性
- コードのモジュール化
- テストカバレッジの向上
- ドキュメントの整備
